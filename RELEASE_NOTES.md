# GaussDB快捷命令管理器 - Release Notes

---

## v6.1.0 (2026-04-28)

### 新功能

- **批量编辑功能**：
  - 工具栏新增"📝 批量编辑"按钮
  - 类似 Excel 的表格编辑界面，可直接编辑所有命令
  - 支持 8 列字段：一级分类、二级分类、名称、通用命令、集中式、分布式、描述、标签
  - 添加行、删除行操作
  - 分类字段支持下拉选择或直接输入新分类名
  - 保存时自动创建新分类

---

## v6.0.0 (2026-04-27)

### 新功能

- **命令按二级分类分组显示**：
  - 新的二级分类换行显示，更清晰的分组效果
  - 每组显示分类标题和命令数量
  - 卡片内不再重复显示分类标签，界面更简洁

---

## v5.9.0 (2026-04-27)

### 修复

- **恢复默认数据彻底修复**：
  - 重写 restoreDefaultData 函数，创建全新数据库而非清理旧数据
  - 先清除 localStorage 缓存，避免损坏数据影响
  - 确保在新环境部署后恢复默认数据正常生效

- **清空数据功能优化**：
  - clearAllData 同样创建全新空数据库
  - 清除 localStorage 缓存确保彻底清空

- **启动脚本优化**：
  - 每次启动都复制 sql.js 文件，确保版本正确
  - 避免旧版本 WASM 文件导致的问题

---

## v5.8.0 (2026-04-27)

### 修复

- **命令排序问题修复**：
  - 修复导入命令后显示顺序错误的问题
  - 排序逻辑改为：sort_order 值小的排前面（符合直觉）
  - 新添加的命令自动排在最后
  - 拖拽排序后顺序正确保存

- **恢复默认数据修复**：
  - 修复在新环境恢复默认数据后分类关联丢失的问题
  - 使用动态获取的分类 ID 代替硬编码 ID
  - 确保命令正确关联到二级分类

---

## v5.7.0 (2026-04-27)

### 新功能

- **命令按分类顺序排列**：命令显示顺序按一级二级分类的排序排列
- **命令卡片显示分类编号**：分类标签显示编号（如 01 Git、01-01 提交相关）

---

## v5.6.0 (2026-04-26)

### 新功能

- **二级分类拖拽排序**：
  - 二级分类显示序号（如 01-01、01-02）
  - 鼠标悬停显示拖拽手柄 `⋮⋮`
  - 二级分类可在父分类内拖拽调整顺序
  - 冻结按钮共用，锁定时一级和二级分类都无法拖拽

---

## v5.5.0 (2026-04-26)

### 新功能

- **新增一级分类**：分类弹窗支持直接创建一级分类（无需选择父分类）

### 优化

- **页面整体美化**：
  - 标题栏简洁渐变设计
  - 工具栏紧凑布局，架构切换按钮优化
  - 搜索框添加搜索图标
  - 侧边栏阴影效果，宽度调整
  - 命令卡片 hover 效果增强
  - 空状态添加图标和快捷按钮

---

## v5.4.0 (2026-04-26)

### 新功能

- **侧边栏一级分类拖拽排序**：
  - 序号显示（01、02、03...）
  - 鼠标悬停显示拖拽手柄 `⋮⋮`
  - 🔒/🔓 冻结切换按钮

- **分类命令数量显示**：
  - 一级分类显示汇总数量
  - 二级分类显示命令数量
  - 数量随数据变化自动刷新

### 优化

- **二级分类颜色渐变**：同一一级分类下的二级分类颜色渐变变化

- **命令卡片更紧凑**：减少留白和间距

- **标签默认不显示**：简化卡片展示

- **工具栏布局调整**：皮肤/导入导出/新增按钮放到最右边

- **导入导出界面美化**：卡片式设计，图标标识

---

## v5.3.0 (2026-04-26)

### 新功能

- **侧边栏全部展开/折叠**：
  - "全部命令"右侧添加切换按钮
  - 点击一键展开或折叠所有二级分类
  - 折叠状态显示 ▶，展开状态显示 ▼

### 优化

- **命令卡片布局更紧凑**：
  - 减少卡片内边距和元素间距
  - 缩小按钮和文字尺寸

- **命令保存后立即刷新显示**：修复修改命令后界面不更新的问题

---

## v5.2.0 (2026-04-26)

### 新功能

- **皮肤切换**：
  - 支持浅色(👔)和深色(🧥)两种主题
  - 点击工具栏右侧按钮一键切换
  - 主题选择保存在 localStorage

- **软件重命名**：更名为"GaussDB快捷命令管理器"

- **默认数据更新**：
  - 更新为 GaussDB 相关命令
  - 一级分类：Git、集群、实时会话
  - 二级分类使用父分类浅色版本

- **Excel 导入颜色自动分配**：
  - 一级分类随机分配预定义颜色
  - 二级分类继承父分类浅色版本

- **清空所有数据**：新增独立清空数据按钮（区别于恢复默认数据）

---

## v5.1.0 (2026-04-26)

### 修复

- **恢复默认数据功能修复**：
  - 修复恢复默认数据后分类关联丢失的问题
  - 删除数据后重置 SQLite 自增 ID（sqlite_sequence），确保分类从 ID 1 开始
  - 按钮文字从"清空所有数据"改为"恢复默认数据"，准确反映功能

---

## v5.0.0 (2026-04-26)

### GaussDB 架构切换功能

专为 GaussDB 数据库快捷命令管理设计，支持集中式和分布式两种架构：

- **全局架构切换**：工具栏新增架构切换按钮组
  - 🔵 集中式 - 显示集中式架构专用命令
  - 🟢 分布式 - 显示分布式架构专用命令
  - ⚪ 全部 - 显示所有命令

- **命令变体存储**：一条命令可存储三种版本
  - 通用命令 - 两种架构都可使用
  - 集中式命令 - 集中式架构专用（优先显示）
  - 分布式命令 - 分布式架构专用（优先显示）

- **智能复制菜单**：点击复制按钮可选择复制不同架构版本
  - 复制集中式命令
  - 复制分布式命令
  - 复制通用命令

- **架构图标标识**：命令卡片显示当前架构图标
  - 🔵 有集中式专用命令
  - 🟢 有分布式专用命令
  - ⚪ 使用通用命令

### Excel 格式变更

- 导出新增：通用命令、集中式命令、分布式命令三列
- 导入支持解析三种命令列

---

## v4.0.0 (2026-04-26)

### UI 优化

- **现代化标题栏**：
  - 新增顶部标题"⚡ 快捷命令管理器"
  - 使用 Unsplash 抽象渐变背景图片
  - 标题居中显示，深色渐变叠加确保文字清晰
  - 工具栏（搜索、按钮）移至标题下方

- **侧边栏交互优化**：
  - 点击一级分类切换展开/折叠二级分类
  - 一级分类图标：圆形 ●
  - 二级分类图标：菱形 ◆
  - 折叠按钮使用 SVG 箭头图标，折叠时旋转 180°
  - 增加 hover 提示和过渡动画

### 功能改进

- **清空数据优化**：清空后自动删除 localStorage 缓存，刷新页面自动生成测试数据

---

## v3.0.0 (2026-04-26)

### 新功能

- **二级分类**：支持创建层级分类结构，更好地组织大量命令
  - 一级分类可展开/折叠显示二级分类
  - 点击一级分类显示其下所有二级分类的命令
  - 命令关联到二级分类

### UI 改进

- **侧边栏树形展示**：一级分类带有展开/折叠按钮，二级分类缩进显示
- **分类弹窗**：新增"父分类"选择，可创建一级或二级分类
- **命令弹窗**：分类下拉改为分组选择（optgroup），一级分类分组显示二级分类选项

### 数据库改动

- categories 表新增 `parent_id` 列
- 新增 `migrateDatabase()` 兼容已有数据自动升级

### Excel 格式变更

- 导出格式：新增"一级分类"和"二级分类"两列
- 导入逻辑：支持解析一级分类/二级分类并自动创建

### 测试数据更新

- Git 分类下新增：提交相关、分支管理
- Docker 分类下新增：容器操作、镜像管理

---

## v2.0.0 (2026-04-26)

### 新功能

- **启动/关闭脚本**：新增 `启动项目.bat` 和 `关闭项目.bat`，支持后台运行和一键关闭
- **Git 版本控制**：项目正式使用 Git 进行版本管理

### 修复

- **Excel 导入分类筛选问题**：修复导入命令在分类筛选下不显示的问题
  - 修复 `addCategory` 返回 ID 为 0 的 bug（last_insert_rowid 获取时机错误）
  - 修复 `addCommand` categoryId 为 0 时未正确处理的问题
  - 新增命令正确设置 sort_order 值，确保显示在列表前端
  - 支持大小写不敏感匹配分类名
  - trim() 处理分类名和命令名，避免空格导致匹配失败

### 改进

- **导入功能增强**：
  - 分类名 trim() 处理，避免空格问题
  - 大小写不敏感匹配已有分类
  - 过滤空命令行，只导入有效数据

### 技术更新

- 创建 `.gitignore` 排除 node_modules、dist 等目录
- 初始化 Git 仓库，建立版本管理规范

---

## v1.0.0 (2026-04-26)

### 首次发布

快捷命令管理器是一个现代化的命令管理工具，帮助用户高效地存储、分类、检索和复用常用命令。

### 新功能

#### 核心功能
- **命令管理**：支持添加、编辑、删除命令
- **分类管理**：支持创建、编辑、删除分类，自定义颜色标识
- **实时搜索**：支持按名称、命令内容、描述、标签搜索
- **一键复制**：点击即可复制命令到剪贴板，带有成功提示

#### 数据管理
- **Excel 导入导出**：支持 .xlsx/.xls 格式数据交换
- **数据库文件导入导出**：支持 .db SQLite 数据库文件备份恢复
- **本地持久化**：数据自动保存到浏览器 localStorage

#### 用户界面
- **Notion 风格**：简洁美观的现代化界面
- **响应式设计**：适配桌面、平板、移动端
- **侧边栏分类**：可折叠的侧边栏，按分类筛选命令
- **Toast 提示**：操作反馈提示

### 技术实现

| 技术 | 版本 | 用途 |
|-----|------|------|
| Vue 3 | 3.4.x | 前端框架 |
| Vite | 5.x | 构建工具 |
| Tailwind CSS | 3.4.x | 样式框架 |
| sql.js | 1.14.x | 浏览器端 SQLite |
| xlsx (SheetJS) | 0.18.x | Excel 处理 |

### 数据结构

**分类表 (categories)**
- id: INTEGER PRIMARY KEY
- name: TEXT (分类名称)
- color: TEXT (颜色)

**命令表 (commands)**
- id: INTEGER PRIMARY KEY
- name: TEXT (命令名称)
- content: TEXT (命令内容)
- category_id: INTEGER (分类ID)
- description: TEXT (描述)
- tags: TEXT (标签)
- sort_order: INTEGER (排序)

### 预置测试数据

- 4 个分类：Git、NPM、Docker、系统
- 19 条常用命令示例

### 项目结构

```
quick_search/
├── public/
│   ├── favicon.svg
│   ├── sql-wasm-browser.js    # sql.js 引擎
│   └── sql-wasm-browser.wasm  # WASM 文件
├── src/
│   ├── components/
│   │   ├── Sidebar/           # 侧边栏
│   │   ├── SearchBar/         # 搜索栏
│   │   ├── CommandCard/       # 命令卡片
│   │   ├── Modal/             # 弹窗组件
│   │   └── common/            # 通用组件
│   ├── utils/
│   │   ├── database.js        # 数据库操作
│   │   ├── excel.js           # Excel 处理
│   │   └── clipboard.js       # 剪贴板
│   ├── App.vue
│   ├── main.js
│   └── style.css
├── index.html
├── package.json
├── vite.config.js
├── tailwind.config.js
└── README.md
```

### 已知限制

- 数据存储在浏览器 localStorage，最大约 5MB
- sql.js WASM 文件首次加载需要网络连接
- 暂不支持拖拽排序（计划 v1.1 实现）
- 暂不支持云端同步（计划后续版本）

### 启动方式

```bash
cd quick_search
npm install
npm run dev
```

或双击 `启动项目.bat` 脚本。

---

## 更新日志模板

后续版本更新请按以下格式记录：

```
## vX.X.X (YYYY-MM-DD)

### 新功能
- 功能描述

### 改进
- 改进描述

### 修复
- Bug修复描述

### 技术更新
- 技术变更描述
```